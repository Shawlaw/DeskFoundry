# Template assets

Replace the icon assets in `icons/` with product-specific files before publishing a real application.

Recommended files:

- `icons/icon.ico` for Windows executable metadata
- `icons/icon_256.png` for GUI window icon / README use

